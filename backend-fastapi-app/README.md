# Family - Tree 
    A custom family three generator project.

# Backend (inside backend-fast-app)
    - Install dependencies using cmd  `uv sync'
    - Run app using cmd `uv run fastapi dev app/main.py`
    - Open browser and visit URL `http://localhost:8000/`